name: CI/CD Tienda Perritos - Backend

on:
  push:
    branches: [ "deploy" ]
    paths:
      - "backend/**"

jobs:
  build-push-deploy-backend:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout código
      uses: actions/checkout@v3

    - name: Configurar credenciales AWS
      uses: aws-actions/configure-aws-credentials@v4
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-session-token: ${{ secrets.AWS_SESSION_TOKEN }}
        aws-region: ${{ secrets.AWS_REGION }}

    - name: Login en Amazon ECR (runner)
      uses: aws-actions/amazon-ecr-login@v1


    - name: Build y Push con Cache
      run: |
        # Entramos a la carpeta del microservicio 
        cd backend
        
        # Traer la imagen anterior para cachear capas de la JVM y dependencias
        docker pull ${{ secrets.ECR_REPO_URL_BACKEND }}:latest || true
        
        # Build optimizado
        docker build \
          --cache-from ${{ secrets.ECR_REPO_URL_BACKEND }}:latest \
          -t tienda-backend .
          
        # Tag y Push
        docker tag tienda-backend:latest ${{ secrets.ECR_REPO_URL_BACKEND }}:latest
        docker push ${{ secrets.ECR_REPO_URL_BACKEND }}:latest


    - name: Desplegar Backend vía SSM
      run: |
        aws ssm send-command \
          --instance-ids ${{ secrets.EC2_BACKEND_INSTANCE_ID }} \
          --document-name "AWS-RunShellScript" \
          --comment "Actualizar tienda-backend desde ECR" \
          --parameters '{"commands":[
            "set -e",
            "aws ecr get-login-password --region '${{ secrets.AWS_REGION }}' | docker login --username AWS --password-stdin ${{ secrets.ECR_REGISTRY }}",
            "docker pull ${{ secrets.ECR_REPO_URL_BACKEND }}:latest",
            "docker stop tienda-backend || true",
            "docker rm tienda-backend || true",
            "docker run -d --name tienda-backend -p 8080:8080 -v despachos_data:/app/data ${{ secrets.ECR_REPO_URL_BACKEND }}:latest"
          ]}' \
          --output text